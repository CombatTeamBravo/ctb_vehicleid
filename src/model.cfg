class CfgModels
{
	class Default;  // Redeclare so we can inherit from it here...
	class ctb_vehicle_idplate_tank: Default
	{
		// Do we need to define a Skeleton???  Hopefully just default will do.
		
		// sections used in the config (and setObjectTexture)
		sections[]= {"v_id0","v_id1", "v_id2"};
		// Not inheriting sections..
		sectionsInherit= "";	  	
	};
};